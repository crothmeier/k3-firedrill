**Stub-sample rule (in force 2026-09-02).** Any stub, mock, or fixture that models the behaviour, output shape, naming, or file surface of an external tool — PVE/`qm`, k3s, etcd/`etcdctl`, systemd, cloud-init, the guest OS — must carry, beside it in the same file, one **measured sample** from the real tool: the exact command, the host it ran on, the date, and the verbatim output or file content the stub reproduces (credentials redacted to prefix+digest per the evidence writer's convention). A stub without a sample is a defect, not a placeholder. Four stubs passed review with the modelled assumption wrong and were disproven on hardware on 2026-09-02: F-A03-1, F-TA-1, F-TA-4, F-TA-6. Each was cheap to measure and expensive to discover live.

Source of the verbatim paragraph above: `AGENTS.md:87` at `28565e4`.

# Mock-model stub inventory — 2026-09-05

UNCOMMITTED. Scope: all 700 lines of `py/firedrill/mock_model.py` at `28565e4`; inventory and test-count archaeology only. No code, samples, existing counts, prompt files, or evidence under repository `runs/` were edited. Initial verification: `git rev-parse --short HEAD` returned `28565e4`; `git status --porcelain` returned no output.

Columns and status vocabulary follow `docs/STUB_INVENTORY_2026-09-04.md:12-109`. Adjacent instances of one surface are grouped; each range starts at its first relevant line. Harness-only helpers are included for completeness. `AMBIGUOUS` preserves the boundary question rather than deciding it. There is no measured sample beside any model in `mock_model.py`, so no row is `SAMPLE`. A cross-file sample can cover a particular assertion without satisfying the same-file rule here. “Covered by” below is limited to the explicitly named behavior, never the entire row.

All commands in sample-status cells are INERT sample requirements, not execution instructions for this dispatch. The named hosts are the rehearsal surfaces from the ledger. No lab command was executed. Mutation sampling would require a separately ruled rehearsal; read-only commands must also remain unexecuted here. For each `etcdctl` command below, the exact executable is `/opt/firedrill/artifacts/etcdctl`, with `ETCDCTL_ENDPOINTS=https://127.0.0.1:2379`, `ETCDCTL_CACERT=/var/lib/rancher/k3s/server/tls/etcd/server-ca.crt`, `ETCDCTL_CERT=/var/lib/rancher/k3s/server/tls/etcd/client.crt`, and `ETCDCTL_KEY=/var/lib/rancher/k3s/server/tls/etcd/client.key`, as reconstructed in `tests/test_pve_driver.sh:173-178`. Placeholders identify reviewed inputs or returned paths; they are not supplied credentials.

All token/digest constructions described as synthetic below are deterministic from `lab_id` (and the stated node/epoch/revision inputs); they are not credentials. No generated token or full digest value is reproduced. `join_credential_sha256` itself can accept arbitrary token input; the deterministic statement applies to values generated by this mock, not arbitrary caller input.

| file:line | tool modelled | what the stub asserts about the tool | disproven? (finding ID or —) | sample status |
|---|---|---|---|---|
| `py/firedrill/mock_model.py:16-23` | k3s token/bootstrap conventions; harness topology | Imported `bootstrap_key_name` and `normalize_token` supply external credential/key semantics; `EXPECTED_NODES`, `EXPECTED_SERVERS`, token references, and full-token guards also supply harness policy. Concrete uses are inventoried below. | — | AMBIGUOUS — imported external semantics and our guard interface; definitions outside this file are not additional inventory targets. |
| `py/firedrill/mock_model.py:26-43` | harness errors and persistence | `MockError` and `atomic_json` implement our exception, JSON/newline, temporary-file, fsync, replace, and cleanup protocol. They do not emulate a guest file or its mode/mtime. | — | N/A — our local model storage. |
| `py/firedrill/mock_model.py:46-49` | k3s server-token and env-file forms | `initial_server_token` generates only `K10<ca>::server:<secret>`, with synthetic 64-hex CA and secret from distinct `lab_id` seeds. There is no separately stored bare-secret env form. | F-TB-1 | NO-SAMPLE — TO RUN read-only on server-1: `grep -c '^K3S_TOKEN=[a-f0-9]\{64\}$' /etc/systemd/system/k3s.service.env`; `grep -c '^K10[a-f0-9]\{64\}::server:[a-f0-9]\+$' /var/lib/rancher/k3s/server/token`. Wording/commands reused from the F-TA-4 surface row in the 09-04 inventory, line 31. |
| `py/firedrill/mock_model.py:52-68` | k3s token proposal/rotation | `server_token_for_epoch`, `current_server_token`, and `proposed_server_token` select synthetic full tokens by epoch. Every nonzero epoch derives a new CA field as well as a new secret; this does not preserve the current token's CA field. No `k3s token generate` bootstrap-token branch exists. | F-TA-1 | NO-SAMPLE — the generator/server-token distinction is covered by `tests/test_pve_driver.sh:851-859` (command reconstructed), not the changed-CA proposal. INERT on server-1: `/usr/local/bin/k3s token generate` with output consumed only by length/shape checks; `/usr/local/bin/k3s token --help`; `sha256sum /var/lib/rancher/k3s/server/tls/server-ca.crt` before/after a separately approved rotation. Ledger `docs/PROJECT_STATE.md:94-104,125-127` records preserved CA and hardware proof. |
| `py/firedrill/mock_model.py:71-72` | k3s credential normalization | `join_credential_sha256` hashes `normalize_token(token)`, so a full token and its bare secret yield the same join-credential digest. It does not record the two on-disk file digests. | F-TB-1 | AMBIGUOUS — our join-label digest uses external token normalization; supporting definition `py/firedrill/bootstrap.py:66-88`. The missing file-form model is the NO-SAMPLE row at line 46. |
| `py/firedrill/mock_model.py:75-77` | etcd datastore identity | `datastore_sha256` hashes `mock-datastore:<lab_id>:<node>:<revision>` as a synthetic content-like marker, not real database bytes or an etcd-reported hash. | — | AMBIGUOUS — own marker standing in for datastore identity; it is not measured disk integrity. |
| `py/firedrill/mock_model.py:80-83` | k3s embedded-etcd member naming | `etcd_member_name` returns `<node>-<8 lowercase hex>` with a stable synthetic suffix derived from `lab_id` and node. The naming shape matches the repaired assumption; the suffix algorithm is invented for the mock. | F-TA-6 / F-TBC-1 | NO-SAMPLE — naming shape covered by `tests/test_pve_driver.sh:168-181` (command reconstructed). INERT on server-1: `etcdctl member list --write-out=simple`. Ledger `docs/PROJECT_STATE.md:178-198`; no sample here proves the synthetic suffix derivation. |
| `py/firedrill/mock_model.py:86-92` | harness model identity | `empty_model` starts schema 3, identity `mock-hypervisor`, the supplied `lab_id`, and an empty guest dictionary. | — | N/A — our model container and sentinel, not a measured PVE hostname. |
| `py/firedrill/mock_model.py:93-99` | k3s CA, server token, etcd bootstrap key | Initial cluster state has one derived bootstrap key, token epoch/reference, rotation count, and normalized join digest. `ca_sha256` uses a separate synthetic `mock-ca` seed from the token's CA field and stays independent of it. | F-TA-1 | NO-SAMPLE — INERT on server-1: `etcdctl get /bootstrap/ --prefix --keys-only`; `sha256sum /var/lib/rancher/k3s/server/tls/server-ca.crt`. Ledger `docs/PROJECT_STATE.md:125-127` records one replaced key and CA continuity; `tests/test_pve_driver.sh:851` covers only the generator distinction. |
| `py/firedrill/mock_model.py:100-106` | harness fault injection | `test_a`, decryption-failure node list, health-delay map, `raw_output`, and `test_c` are our scenario controls. The names select external effects inventoried at their consumers. | — | N/A — our injection protocol. |
| `py/firedrill/mock_model.py:111-135` | harness migration over k3s model fields | `migrate_model` removes legacy raw `server_token`, defaults epoch from rotation count, regenerates token/key/digest defaults, and supplies scenario controls. It silently synthesizes missing external-state observations for older mock files. | — | AMBIGUOUS — our schema migration carrying the token/bootstrap assumptions above. |
| `py/firedrill/mock_model.py:136-151` | PVE power, k3s readiness/credential, etcd health | Missing guest fields default Ready and k3s-running from installed plus `power == running`; server health adds role `server`. Installed nodes inherit the current cluster credential and running-token reference, without an independent env source or `server/token` file. | F-TA-4, F-TB-1 | NO-SAMPLE — regeneration dependency covered by `tests/test_pve_driver.sh:203-223` (commands reconstructed). INERT on server-2: `systemctl show k3s -p ActiveState -p NRestarts`; `k3s kubectl get nodes -o json`; `grep -c '^K3S_TOKEN=[a-f0-9]\{64\}$' /etc/systemd/system/k3s.service.env`; `grep -c '^K10[a-f0-9]\{64\}::server:[a-f0-9]\+$' /var/lib/rancher/k3s/server/token`. |
| `py/firedrill/mock_model.py:152-160` | etcd revision/snapshots, systemd journal/restarts | Migration assigns datastore revision 1 to installed servers, otherwise 0; synthesizes its marker; defaults journal to `[]`, etcd snapshots to `{}`, and `restart_count` to 0. No service unit state, `NRestarts`, credential-file mode, or mtime is stored here. | — | NO-SAMPLE — INERT on server-1: `etcdctl endpoint status --write-out=json`; `systemctl show k3s -p ActiveState -p NRestarts`; `journalctl --unit k3s --no-pager --output=cat`; `stat -c '%n %F %a %U:%G %Y' /var/lib/rancher/k3s/server/cred/passwd /var/lib/rancher/k3s/server/token`. |
| `py/firedrill/mock_model.py:161-187` | harness schema and guest lookup | Schema version assignment, `load_model`, `guest`, and `guest_by_name` validate local model/lab identity and exactly one matching node; missing-guest diagnostics are ours. | — | N/A — our persistence and lookup contract, not `pvesh` error text. |
| `py/firedrill/mock_model.py:190-195` | PVE guest config, cloud-init naming | `new_guest` copies the entire supplied spec, uses `short_name`, assigns owner `lab_id`, and starts `power=stopped`. PVE IDs/names/role/network/cloud-init inputs remain opaque spec fields; no `qm config` or cloud-init output is synthesized. | — | AMBIGUOUS — harness inventory copied into guest state. INERT comparison surface: on lab-hv01, `pvesh get /nodes/lab-hv01/qemu/7100/config --output-format json`; `qm cloudinit dump 7100 user`; `qm cloudinit dump 7100 network`. |
| `py/firedrill/mock_model.py:196-207` | k3s/etcd installation, systemd, guest snapshots | New guests are uninstalled, not Ready, unhealthy, not k3s-running, with null credential references, revision 0 and a synthetic datastore marker, empty journals and both snapshot dictionaries, restart count 0. These fields do not model real file ownership/modes/mtimes. | — | NO-SAMPLE — INERT on freshly cloned server-1: `systemctl show k3s -p LoadState -p ActiveState -p NRestarts`; `stat -c '%n %F %a %U:%G %Y' /var/lib/rancher/k3s/server/db`; on lab-hv01: `qm status 7100`; `qm listsnapshot 7100`. |
| `py/firedrill/mock_model.py:211-227` | k3s Kubernetes node observation | `cluster_state` sorts guests by numeric inventory `id`; emits `{name, role, ready, join_credential_sha256}`; Ready is the conjunction of VM power, installation, k3s-running, and a stored Ready bit. | — | NO-SAMPLE — INERT on server-1: `k3s kubectl get nodes -o json`; `k3s kubectl get --raw=/readyz`. JSON here is the harness projection, not verbatim Kubernetes API output. |
| `py/firedrill/mock_model.py:229-237` | etcd member/endpoint health | Only role `server` produces `{name, healthy}`; suffixed member name is derived by the mock; health requires the combined Ready predicate and `etcd_healthy`. | F-TA-6 / F-TBC-1 | NO-SAMPLE — naming shape covered by `tests/test_pve_driver.sh:168-181`; health coupling is not. INERT on server-1: `etcdctl member list --write-out=simple`; `etcdctl endpoint health --cluster`. |
| `py/firedrill/mock_model.py:238-245` | harness cluster projection of k3s/etcd | Output includes node/member arrays, `all_nodes_ready` only for exactly five nodes, `all_etcd_members_healthy` only for exactly three members, bootstrap-key list and CA digest. Five/three is harness topology, not a general k3s limit. | — | AMBIGUOUS — our aggregate observation schema with external-state fields already inventoried. |
| `py/firedrill/mock_model.py:249-267` | PVE snapshot guest state | `snapshot_payload` saves VM power, installation/readiness/etcd health, join/running-token references, datastore revision/marker, journal, etcd snapshots, restart count, and the entire shared cluster. This models restorable memory-like service state even for the disk-only rehearsal snapshot. | F-A03-1 | NO-SAMPLE — disk-only rollback behavior covered by `tests/test_pve_driver.sh:554-574`; it contradicts restoring running power automatically. INERT on lab-hv01 in a separately approved rehearsal: `qm rollback 7100 p06-baseline`; then `qm status 7100`. |
| `py/firedrill/mock_model.py:270-286` | harness snapshot-pair guards | `capture_paired_snapshot` requires a full, exact current server token, an expected server name, and an installed/Ready guest before mutation. Error text and pairing prerequisites are ours. | — | N/A — harness safety policy, not an assertion that `k3s etcd-snapshot` enforces these guards. |
| `py/firedrill/mock_model.py:288-301` | `k3s etcd-snapshot`, guest filesystem | Snapshot path is always `/var/lib/rancher/k3s/server/db/snapshots/test-a-<node>.db`; synthetic SHA input is lab/node/token-reference/CA rather than snapshot bytes. Pair shape is `{node, path, snapshot_sha256, server_token_reference}`; a matching existing pair is a no-op, a different pair at that path errors. | — | NO-SAMPLE — INERT on server-1 in a separately approved rehearsal: `k3s etcd-snapshot save --name <sample-name>`; then `find /var/lib/rancher/k3s/server/db/snapshots -maxdepth 1 -type f -name '<sample-name>*' -print`; `sha256sum <returned-path>`; `stat -c '%n %F %a %U:%G %Y' <returned-path>`. No real file or mode/mtime is modeled here. |
| `py/firedrill/mock_model.py:303-316` | harness Test A snapshot bookkeeping | Initializes snapshot-pair map, rotated flag, expected restart order and event list; refuses a post-rotation capture; stores copies in guest and cluster maps. | — | N/A — our pairing/phase protocol. |
| `py/firedrill/mock_model.py:319-343` | harness rotation guards over k3s token | `rotate_server_token` requires the full token, an exact three-server permutation, three current paired snapshots, a changed token, a single rotation and exactly the synthetic next-epoch proposal. These are stricter than a modeled k3s CLI interface. | — | N/A — our test contract; the proposal's external CA assumption is inventoried at line 52. |
| `py/firedrill/mock_model.py:344-348` | `k3s token rotate`, etcd bootstrap data | Rotation advances epoch/reference/count, replaces the entire bootstrap-key set with one key derived from the new token, and replaces the cluster join digest; guests remain unchanged until restart. No env-file or token-file write occurs in the model. | F-TA-1, F-TA-4 | NO-SAMPLE — source/regeneration behavior covered by `tests/test_pve_driver.sh:203-223`; generator distinction by `:851-859`. INERT on server-1 in a separately approved rehearsal: `k3s token rotate --new-token <redacted-approved-token>`; then `etcdctl get /bootstrap/ --prefix --keys-only`. Ledger `docs/PROJECT_STATE.md:125-127,175-177` records rotation/restart proof. |
| `py/firedrill/mock_model.py:349-357` | harness rotation evidence | Records rotated flag, configured restart sequence, empty events, and `server-token-rotated` JSON with token reference and bootstrap keys. | — | N/A — our event schema; not native `k3s token rotate` stdout. |
| `py/firedrill/mock_model.py:360-386` | harness restart ordering | `restart_node` validates a positive timeout, known node, completed rotation, and the configured server order followed by literal `agent-1`, `agent-2`; requires an installed, running guest. | — | N/A — harness choreography, not systemd ordering semantics. |
| `py/firedrill/mock_model.py:387-406` | systemd/k3s restart and journal | Restart clears Ready/k3s-running/server-health, increments `restart_count` once, appends a synthetic stop line, and uses injected convergence time (default 1 second). Exceeding the bound adds a synthetic timeout journal line and returns status `timeout`, exit 70. There is no automatic restart loop or `NRestarts` field. | F-TA-4 | NO-SAMPLE — activating/automatic-restart behavior is covered by `tests/test_pve_driver.sh:203-217`, not this counter or timing. INERT on server-2: `systemctl show k3s -p ActiveState -p SubState -p NRestarts`; `journalctl --unit k3s --no-pager --output=cat`. Actual timing requires a separately approved `systemctl restart k3s`. |
| `py/firedrill/mock_model.py:408-414` | k3s service credential, Ready and etcd convergence | Within the time bound, restart unconditionally sets Ready, k3s-running, server etcd health, running-token reference and join digest to current cluster values. It never checks or repairs an independently stale env source; no `server/token` regeneration or `cred/passwd` content/mtime is represented. | F-TA-4, F-TB-1 | NO-SAMPLE — source dependency/regeneration covered by `tests/test_pve_driver.sh:203-223`. INERT on server-2: `grep -c '^K3S_TOKEN=[a-f0-9]\{64\}$' /etc/systemd/system/k3s.service.env`; `grep -c '^K10[a-f0-9]\{64\}::server:[a-f0-9]\+$' /var/lib/rancher/k3s/server/token`; `stat -c '%n %F %a %U:%G %Y' /var/lib/rancher/k3s/server/cred/passwd /var/lib/rancher/k3s/server/token`. |
| `py/firedrill/mock_model.py:415-419` | k3s bootstrap failure journal and timestamp trap | The only injected failure text is `failed to decrypt bootstrap data: cipher: message authentication failed`, appended after health is set true. No stale-token comparison, wrong-token file rewrite, `newer than datastore` condition, or exact `different token` signature exists in this function. | F-TB-3 | NO-SAMPLE — INERT on server-2 after the ruled wrong-token scenario: `journalctl --unit k3s --no-pager --output=cat`; `stat -c '%n %Y' /var/lib/rancher/k3s/server/cred/passwd /var/lib/rancher/k3s/server/token`; `systemctl show k3s -p ActiveState -p NRestarts`. Commands reused from `docs/STUB_INVENTORY_2026-09-04.md:78`. Related signatures are covered by `tests/test_pve_driver.sh:203-217`; that sample does not establish the later F-TB-3 causal mtime observation. Ledger `docs/PROJECT_STATE.md:206-210`. |
| `py/firedrill/mock_model.py:420-432` | harness restart diagnostics | Returns `sequence`, node, status `healthy`, timeout and convergence time; optional `modeled_diagnostic` copies raw injected output. It can return healthy plus an injected journal failure, leaving adjudication to the gate. | — | N/A — our event/fault-injection interface, not native service output. |
| `py/firedrill/mock_model.py:435-453` | k3s journal; harness gate projection | `gate_observation` combines cluster state, server-name-to-journal-list map, restart events, snapshot pairs, and token reference under event `test-a-gate-observation`. Journals are copied only for `EXPECTED_SERVERS`, not agents. | — | AMBIGUOUS — our evidence schema contains external journal/state claims. INERT on server-1, server-2 and server-3: `journalctl --unit k3s --no-pager --output=cat --lines 200`. |
| `py/firedrill/mock_model.py:455-458` | harness diagnostics | Gate `modeled_diagnostic` copies optional raw injection; it does not invent a separate external-tool response. | — | N/A — our redaction-testing interface. |
| `py/firedrill/mock_model.py:461-468` | `qm rollback` and cluster persistence | `restore_snapshot` copies all saved guest fields, or legacy `power`/`installed`, then replaces the shared cluster. A snapshot taken running restores running power/service state; there is no stop-before/after beat or distinction between disk and VM-memory snapshots. | F-A03-1 | NO-SAMPLE — contrary disk-only power behavior covered by `tests/test_pve_driver.sh:554-574`. INERT on lab-hv01 in a separately approved rehearsal: `qm rollback 7100 p06-baseline`; then `qm status 7100`. |
| `py/firedrill/mock_model.py:471-478` | PVE host/template discovery | `handle` answers `identity` with `mock-hypervisor`, and `template-exists` always prints `true`/returns 0 without a model, template ID or config check. Sentinel output is ours; existence stands in for PVE. | — | AMBIGUOUS — our discovery protocol with unconditional external existence. INERT on lab-hv01: `hostname`; `pvesh get /nodes/lab-hv01/qemu/9000/config --output-format json`. |
| `py/firedrill/mock_model.py:479-488` | PVE existence/ownership projection | `exists` means a local model and guest dictionary key exist (exit 0/1); `owner` prints the stored owner string. There is no native PVE description/ownership-marker parsing. | — | AMBIGUOUS — harness ownership/existence interface over PVE-like identity. INERT on lab-hv01: `pvesh get /nodes/lab-hv01/qemu/7100/config --output-format json`. |
| `py/firedrill/mock_model.py:489-502` | PVE guest creation/cloud-init configuration | `create` parses JSON spec, keys by stringified ID and compares all desired fields for a no-op; outputs `already-present` or `created`. It inserts a guest without clone/disk/cloud-init progress, task IDs or errors. | — | AMBIGUOUS — our idempotent create protocol around external guest creation; names/output are not claimed as native `qm` output. INERT on lab-hv01: `pvesh get /nodes/lab-hv01/qemu/7100/config --output-format json`; `qm cloudinit dump 7100 user`. |
| `py/firedrill/mock_model.py:503-507` | `qm start` | `start` immediately sets power `running`, marks changed and prints `started`; it does not itself restore k3s/Ready/etcd flags or wait for boot/cloud-init. | — | NO-SAMPLE — INERT on lab-hv01 in a separately approved rehearsal: `qm start 7100`; then `qm status 7100`; on server-1 after boot: `cloud-init status --long`; `systemctl is-active k3s`. The word `started` is harness output. |
| `py/firedrill/mock_model.py:508-515` | `qm shutdown` / `qm stop` | `stop` immediately sets power stopped and clears Ready/k3s-running/etcd-health; prints `stopped`. No ACPI wait, failure or forced-stop distinction is modeled. | — | NO-SAMPLE — INERT on lab-hv01 in a separately approved rehearsal: `qm shutdown 7100 --timeout <seconds>`; `qm status 7100`; forced-stop sampling separately requires `qm stop 7100`. |
| `py/firedrill/mock_model.py:516-521` | PVE guest status/deletion | `status` returns compact JSON of the entire harness guest record; `delete` removes the dictionary entry immediately and prints `deleted`, without native task progress, stopped prerequisite or snapshot cleanup. | — | AMBIGUOUS — our status/delete interface wrapping PVE semantics. INERT on lab-hv01: `qm status 7100`; `pvesh get /nodes/lab-hv01/qemu/7100/status/current --output-format json`. A removal sample may only come from the separately authorized harness teardown. |
| `py/firedrill/mock_model.py:522-531` | PVE snapshot creation/existence | Snapshots are name-keyed entries holding `snapshot_payload`; repeat names return `already-present`, new snapshots `snapshotted`; existence is exit 0/1. There is no task, disk/memory option, file format or snapshot-tree parent. | — | NO-SAMPLE — INERT on lab-hv01 in a separately approved rehearsal: `qm snapshot 7100 <sample-name>`; then `pvesh get /nodes/lab-hv01/qemu/7100/snapshot --output-format json`; `qm listsnapshot 7100`. Printed words are harness output. |
| `py/firedrill/mock_model.py:532-542` | `qm rollback` | `snapshot-restore` checks snapshot existence, calls `restore_snapshot` and prints `restored`; no power guard or start phase occurs. | F-A03-1 | NO-SAMPLE — disk-only rollback behavior covered by `tests/test_pve_driver.sh:554-574`. INERT on lab-hv01 in a separately approved rehearsal: `qm rollback 7100 p06-baseline`; then `qm status 7100`. |
| `py/firedrill/mock_model.py:543-562` | guest OS, k3s installer, etcd initialization | `guest-exec install-k3s` requires running VM power, instantly sets installed/Ready/service/health and current credentials; server datastore revision becomes exactly 1 with a synthetic marker. It appends `k3s <role> installed and Ready` and prints installation text using the requested argument. It models no installer files, units, modes, timestamps or cloud-init completion. | — | NO-SAMPLE — INERT on server-1 after a separately approved staged installation: `systemctl show k3s -p ActiveState -p NRestarts`; `k3s kubectl get nodes -o json`; `etcdctl endpoint status --write-out=json`; `stat -c '%n %F %a %U:%G %Y' /etc/systemd/system/k3s.service.env /var/lib/rancher/k3s/server/token`. |
| `py/firedrill/mock_model.py:563-566` | guest reachability and k3s readiness | `wait-access` succeeds whenever guest power is running; `wait-ready` additionally checks a Ready bit. Both print `ready` with no transport, cloud-init or timeout sampling. | — | AMBIGUOUS — our wait hooks stand in for guest readiness. INERT on server-1: `cloud-init status --long`; `k3s kubectl get --raw=/readyz`. |
| `py/firedrill/mock_model.py:567-591` | harness observation/Test A verbs | `capture-cluster-state`, `cluster-state`, `test-a-read-token`, `test-a-propose-token`, `test-a-snapshot`, `test-a-rotate`, `test-a-restart`, `test-a-observe` serialize/delegate to inventoried models. Token verbs print full generated tokens; JSON uses compact separators, some sorted keys; failed restart output goes to stderr. Unsupported guest operations error. | — | AMBIGUOUS — wrapper verbs are ours; token/key/restart/snapshot semantics are external models in the rows above. No native `qm`/`pvesh` stdout equivalence is implied. |
| `py/firedrill/mock_model.py:592-597` | harness decryption-failure injection | Adds one named server to the failure list and prints an injection confirmation. The modeled journal text is at lines 415-419. | — | N/A — our fault selector. |
| `py/firedrill/mock_model.py:598-604` | etcd bootstrap-key namespace | `inject-bootstrap-key` accepts only `/bootstrap/` plus exactly 12 lowercase hex characters and inserts a new key into the list; duplicate key names are a no-op. It models names/existence, not etcd key values, revisions or transaction behavior. | — | NO-SAMPLE — INERT on server-1: `etcdctl get /bootstrap/ --prefix --keys-only`. Derivation is imported from `py/firedrill/bootstrap.py:91-95`; injection confirmation is harness output. |
| `py/firedrill/mock_model.py:605-618` | harness injection/persistence protocol | `inject-health-delay` stores seconds per node; `inject-raw-output` stores arbitrary diagnostics; unsupported commands error; changed state alone is persisted by `atomic_json`. | — | N/A — our scenario and save interface. |
| `py/firedrill/mock_model.py:621-640` | harness CLI with PVE-like lifecycle names | `add_lab_id` and `build_parser` define required `--model`, subcommand selector, identity/template/existence/owner/status/create/start/stop/delete names, integer guest IDs and JSON `--spec`. These are the mock's own grammar, not `qm`/`pvesh` syntax. | — | AMBIGUOUS — lifecycle nouns overlap external tools; effects are separately inventoried at lines 471-521. |
| `py/firedrill/mock_model.py:641-655` | harness snapshot/guest/Test A CLI | Defines snapshot create/exists/restore name arguments, guest-exec operation plus arbitrary operation arguments, cluster-state and token read/propose/observe verbs. No snapshot-delete verb exists in this file. | — | AMBIGUOUS — external operations exposed through our parser, not native CLI compatibility. |
| `py/firedrill/mock_model.py:656-680` | harness Test A and injection CLI | Defines snapshot `--node/--token`, rotation `--token/--restart-order`, restart `--node/--timeout`, failure/key/delay/raw-output controls and expected-node/server choices. Credential arguments here are the local mock protocol; they do not establish a real k3s token input channel. | — | N/A — our parser/guard configuration. |
| `py/firedrill/mock_model.py:681-700` | harness entrypoint/error protocol | Parser return, `main`, exception list, `mock driver error:` stderr, exit 69 and `SystemExit` belong to the harness. Imports/module description at lines 1-14 contain no additional external output fixture. | — | N/A — our process interface. |

## Token forms and timestamp trap

1. **Only one stored/generated token form is modeled.** `initial_server_token` and `server_token_for_epoch` return the full `K10<ca>::server:<secret>` form (`py/firedrill/mock_model.py:46-57`); current/proposed accessors select it (`:60-68`). Normalization can handle a bare secret for hashing (`:71-72`, imported implementation `py/firedrill/bootstrap.py:66-88`), but there is no separate `k3s.service.env` bare-secret value or `server/token` file state. Migration and successful restart copy cluster references/digests directly (`:136-151,408-414`). That distinction matters: support for normalizing two input forms is not a model of the two on-disk forms. The line-46 row is F-TB-1 / NO-SAMPLE with the required read-only shape commands; F-TA-4's source/regeneration sample remains at `tests/test_pve_driver.sh:203`.

2. **`restart_node` models neither the timestamp trap nor the exact `different token` crash signature.** It toggles health, increments a per-call counter and compares an injected delay to the timeout (`py/firedrill/mock_model.py:387-406`), then sets health/current credential references before optionally appending the different decryption message quoted in the line-415 row (`:408-419`). It has no independent stale token source, `cred/passwd`, `server/token`, file modes/mtimes, wrong-token rewrite-before-failure transition, or `newer than datastore` comparison. F-TB-3 / NO-SAMPLE is therefore explicit, with the journal/stat/systemd commands from the 09-04 inventory. `tests/test_pve_driver.sh:203-217` contains related historical signatures, while the later wrong-start/mtime observation is recorded in `docs/PROJECT_STATE.md:206-210`; its k3s source-level mechanism remains UNVERIFIED. No claims are made here about the separately implemented Test B/C models.

## Test count 109 → 141

Read-only archaeology used `git log --stat --reverse 6363cb5..e4b9fbe -- tests/` and per-commit `git show <commit>:tests/run.sh`, followed by `git show <commit>:tests/<file>` for every Python test file and the runner's probe/shell programs. No checkout was performed. The runner invokes unittest discovery with `-p 'test_*.py'`, then standalone Test B/C probes and shell suites; from `632336c` onward it additionally invokes `tests/test_evidence_exec_failure.sh`. None of those standalone programs reports an aggregate assertion/test total; named PASS/negative-probe markers are not an additional unittest count. Thus no all-program grand total is reproducible or claimed. The table gives runner-discoverable unittest methods and, in parentheses, the requested raw `def test_` count. AST inspection of these files identifies direct `unittest.TestCase` methods and the one top-level helper `tests/test_test_b.py:38` (`test_config`), which discovery excludes. This is static counting from committed source, not execution of historical commits. Delta is against the preceding tests-touching commit, with baseline `6363cb5` = 109 discoverable / 110 raw; no intervening commit changes `tests/`.

| commit | date | tests/ files touched (+/−) | count stated in message or docs | count you measure at that commit | delta | what was added (one line) |
|---|---|---|---|---|---|---|
| `632336c` | 2026-09-02 | `tests/run.sh` +1/−0; `tests/test_evidence.py` +54/−0; `tests/test_evidence_exec_failure.sh` +97/−0; `tests/test_pve_driver.sh` +115/−0 | 110 in `632336c:docs/PROJECT_STATE.md:89`; message has no suite total | 110 unittest methods (111 raw) | +1 | Nested transport-failure evidence unit case; exit-255 shell probe; stop/rollback/start ordering probes and runner entry. |
| `1181b50` | 2026-09-02 | `tests/test_pve_driver.sh` +40/−14 | 110 in `1181b50:docs/PROJECT_STATE.md:110`; message has no suite total | 110 unittest methods (111 raw) | 0 | Token proposal transport probes preserve current CA/user, reject generator shape, read failures and collisions. |
| `f2a1f90` | 2026-09-02 | `tests/test_config.py` +17/−1; `tests/test_provision_identity.py` +260/−0; `tests/test_test_a.py` +101/−12; `tests/test_test_a_negative.sh` +54/−0 | 133 in message and `f2a1f90:docs/PROJECT_STATE.md:155` | 133 unittest methods (134 raw) | +23 | Twenty identity/state tests, one config test, two baseline prerequisite tests, plus identity lifecycle/drift probes. |
| `4fda3de` | 2026-09-02 | `tests/test_evidence_exec_failure.sh` +113/−2; `tests/test_pve_driver.sh` +99/−7 | 133 in message and `4fda3de:docs/PROJECT_STATE.md:215` | 133 unittest methods (134 raw) | 0 | Server env-source/regeneration transport cases and shell-death evidence probes. |
| `ef6d921` | 2026-09-02 | `tests/test_mock_lifecycle.sh` +53/−5; `tests/test_test_a.py` +36/−0; `tests/test_test_a_negative.sh` +77/−0 | 135 in message and `ef6d921:docs/PROJECT_STATE.md:185` | 135 unittest methods (136 raw) | +2 | Fresh/leftover phase-ledger unit cases and re-attempt archival/collision shell coverage. |
| `e4b9fbe` | 2026-09-02 | `tests/test_test_a.py` +97/−0 | 141 in message and `e4b9fbe:docs/PROJECT_STATE.md:219` | 141 unittest methods (142 raw) | +6 | Suffixed member-name continuity, missing/fourth member, unexpected prefix, malformed suffix and bare-name compatibility. |

The earliest committed documentation statement of 109 found across available local refs is `ab380ca` (2026-09-01), adding `docs/HANDOFF_2026-09-01_P06_A0_APPEND.md:12` and `:29`; it attributes the measurement to `6363cb5`. The earlier-dated takeover filename received its 109 text later, in `fa07fec`; `42c6f85` subsequently added the “109 → more” dispatch heading. No commit message or `PROJECT_STATE.md` revision in `6363cb5..e4b9fbe` says 109, as the dispatcher observed. The handoff attribution is now corroborated by committed runner/source counting: **109 + (1 + 0 + 23 + 0 + 2 + 6) = 141**. Raw definitions instead give **110 + 32 = 142** because the same top-level `test_config` helper is present throughout, not because of shell probes. The gap after accounting for discovery is zero; `docs/PROJECT_STATE.md:239` is consistent with this measurement. Historical execution itself has not been replayed; the 28565e4 baseline below supplies current runtime evidence. No count was edited elsewhere.

## Executed baseline and limits

Executed once on 2026-09-05 from the repository at HEAD `28565e4`, exact invocation:

```sh
make test
```

Exit status: 0. Runner-reported total: `Ran 141 tests in 0.176s`. Exact last five lines of combined stdout/stderr:

```text
firedrill: command failed at line 17 with exit status 69
NEGATIVE PROBE pve-test-a-no-baseline: expected state failure exit=66 after preflight transport
state error: state file not found: /private/var/folders/7j/2qz1gr5x3n5dlh9dwct9b24m0000gn/T/firedrill-pve-driver.5980Tl/pve-runs/current.json
firedrill: command failed at line 28 with exit status 66
PASS: PVE driver transport-stub suite completed without network access
```

The final exit-69/66 messages belong to expected negative probes, as shown by their adjacent markers; the complete command exited 0. This proves the offline baseline only. The runner gives no combined numerical total for its standalone probes/shell suites. A read-only before/after comparison of all 510 repository `runs/` entries (metadata plus file-content digest prefixes) found no changes. Temporary mock run directories used by the existing tests are outside repository `runs/`.

FINDINGS within increment 1: missing same-file samples remain on every NO-SAMPLE surface; F-TB-1's two file forms and F-TB-3's timestamp transition are absent; the epoch proposal changes its token CA field despite F-TA-1's preserved-CA evidence; disk-only snapshot restore can restore running power despite F-A03-1. Existing sample annotations were referenced, not copied or repaired. These findings do not block the authorized inventory.

Increment 2 has no count finding: 109 → 141 closes and the current runner reports 141. Both increments are delivered. Could NOT execute: new hardware samples or real lifecycle/token/recovery operations, because this dispatch grants no lab access. Remains UNPROVEN: same-file sample compliance, actual external-tool behavior beyond the cited historical evidence, the F-TB-3 source-level mechanism, and any repair to the inventoried gaps. No additional operator ruling was needed to deliver these two increments; no remedy was implemented.
